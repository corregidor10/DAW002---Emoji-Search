import './NoResults.css';

export const NoResults = () => {
    return (
        <div className="component-no-results">
            <span>No se han encontrado resultados</span>
        </div>
    );
};
